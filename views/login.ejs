<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <style>
      /* Reset some default styles for better consistency across browsers */
body, h1, p, form, label, input, button {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* Apply general styles to the entire page */
body {
  font-family: Arial, sans-serif; /* Define a default font family */
  background-color: #f0f0f0; /* Set a background color */
  text-align: center; /* Center-align content in the body */
  padding: 20px; /* Add some padding around the content */
}

/* Style the page title (h1) */
h1 {
  color: #333; /* Set a text color for the title */
  font-size: 28px; /* Adjust the font size as needed */
  margin: 20px 0; /* Add spacing above and below the title */
}

/* Style the welcome message (p) */
p {
  color: #666; /* Set a text color for the welcome message */
  font-size: 18px; /* Adjust the font size as needed */
  margin: 10px 0; /* Add spacing above and below the message */
}

/* Style the form container */
form {
  width: 300px; /* Adjust width as needed */
  margin: 0 auto; /* Center the form horizontally */
}

/* Style form labels */
label {
  display: block; /* Labels on separate lines */
  margin-bottom: 5px; /* Spacing between labels and input fields */
  color: #333; /* Text color for labels */
}

/* Style form input fields */
input[type="text"],
input[type="password"] {
  width: 100%; /* Full width of the form container */
  padding: 10px; /* Adjust padding as needed */
  margin-bottom: 10px; /* Spacing between input fields */
  border: 1px solid #ccc; /* Border styling */
  border-radius: 4px; /* Rounded corners */
}

/* Style the login button */
button[type="submit"] {
  background-color: #007bff; /* Button background color */
  color: #fff; /* Text color */
  padding: 10px 20px; /* Adjust padding as needed */
  border: none;
  border-radius: 4px;
  cursor: pointer;
}

/* Apply styles to the message area (e.g., for displaying login status) */
#message {
  text-align: center;
  color: #ff0000; /* Red text color for error messages */
}

    </style>
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <form method="post" action = "">
        <label>Username</label>
        <input type="text" placeholder="Enter Username" name="username" required>
        <label>Password</label>
        <input type="password" placeholder="Enter Password" name="password" required>
        <button type="submit">Login</button>
        <%- message %> 
      </form>
  </body>
</html>